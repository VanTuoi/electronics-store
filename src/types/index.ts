export type Product = {
    id: string;
    nameProduct: string;
    price: number;
    category: string;
    imageUrl: string;
};

export interface User {
    id: string;
    email: string;
    name: string;
}
