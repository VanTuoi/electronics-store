export interface Product {
    id: string;
    nameProduct: string;
    price: number;
}

export interface ProductCardProps {
    id: string;
    name: string;
    price: string;
    category: string;
    imageUrl: string;
}
